import { ZoneSearch1Pipe } from './zone-search-1.pipe';

describe('ZoneSearch1Pipe', () => {
  it('create an instance', () => {
    const pipe = new ZoneSearch1Pipe();
    expect(pipe).toBeTruthy();
  });
});
